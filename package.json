{
  "name": "group-member-list",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:mail": "maildev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "jest",
    "prisma": "prisma",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "ci:test": "npm run prisma:generate && npm run prisma:migrate:deploy && npm run build && npm start & sleep 10 && jest"
    "prisma:studio": "prisma studio",
    "reset": "prisma migrate reset --force --skip-seed",
    "rest": "npm run reset && npm run prisma:generate && npm run build"
  },
  "dependencies": {
    "next": "^15.5.12",
    "nodemailer": "^7.0.7",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@prisma/client": "^5.22.0",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/nodemailer": "^7.0.9",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.1.6",
    "maildev": "^2.2.1",
    "prisma": "^5.22.0",
    "tailwindcss": "^4",
    "typescript": "^5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "@types/jest": "^29.5.12",
    "prettier": "^3.2.5"
  }
}
